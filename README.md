wip (im not updating this shi regularly cuz i have no time lol maybe only weekends for fun)

first:
 * use proxy if possible (add options too)
 * use resolvers to check if a domain is real
 * let user change max retries (of course it is still limited by code it self)
 * add multiple sources (alienvault, etc...)

its a tool for finding subdomains of a domain via various ways.
